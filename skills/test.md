# test skill

After implementing any feature or component:

1. Write unit tests for all new functions/hooks.
2. Add integration tests if database or API is involved.
3. Run tests and ensure all pass.
4. If testing UI, include accessibility checks (axe-core or similar).
